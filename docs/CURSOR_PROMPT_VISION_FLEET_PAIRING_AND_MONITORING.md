# (초안 폐기) Vision Fleet pairing prompt

**SSOT:** [`docs/VISION_FLEET_SSOT.md`](VISION_FLEET_SSOT.md)

이 파일은 역사적 초안이다. API는 `/vision-fleet/v1`가 아니라 **`/v1`** 이다. Live Wall은 Phase D. 구현은 SSOT + `supabase/functions/vision-fleet` + `src/pages/VisionFleet*.tsx`를 따른다.
