/**
 * Re-export so generate-risk-ai Dashboard/legacy imports keep working.
 * Implementation lives in ../_shared/openaiChat.ts (also used by gemini.ts).
 */
export * from "../_shared/openaiChat.ts";
