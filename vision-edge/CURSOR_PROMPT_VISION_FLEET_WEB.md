# (초안 폐기) Vision Fleet web prompt

**SSOT:** [`../docs/VISION_FLEET_SSOT.md`](../docs/VISION_FLEET_SSOT.md)

Gateway 패키지(`vision-edge/`)는 SafeNex `src/`를 import하지 않는다. Fleet 웹은 `src/pages/VisionFleet*.tsx`만 수정한다.
